# Workspace root: tool configuration only. Installable packages live in packages/.

[tool.pytest.ini_options]
testpaths = ["tests", "packages"]

[tool.ruff]
line-length = 100
target-version = "py310"
