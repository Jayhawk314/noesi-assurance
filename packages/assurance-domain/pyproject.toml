[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "assurance-domain"
version = "0.1.0"
description = "Pure audit domain: entities, identities, state machines, commands. No I/O."
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
