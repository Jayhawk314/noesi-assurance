[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "structural-adapters"
version = "0.1.0"
description = "Explicitly owned ports of the KOMPOSOS-derived structural methods the audit procedures require: composition reachability and Dempster-Shafer fusion."
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
